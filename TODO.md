- [ ] Monte Carlo Simulation – call `/api/finance/monte-carlo` (needs number of runs) and display `MonteCarloChart`
- [ ] Break-Even Heatmap – request timelines + down-payment ranges, call `/api/finance/heatmap`, render `BreakEvenHeatmap`
- [ ] Scenario Overlay – collect multiple scenario inputs, call `/api/finance/scenarios`, feed `ScenarioOverlayChart`
- [ ] Sensitivity Chart – add delta controls (interest, price, rent), call `/api/finance/sensitivity`


